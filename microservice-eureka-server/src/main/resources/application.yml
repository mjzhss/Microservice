spring:
  application:
    name: eureka-server

server:
  address: localhost
  port: 8761

eureka:
  client:
      register-with-eureka: false
      fetch-registry: false
      service-url:
            defaultZone: http://localhost:8761/eureka/
logging:
  file: elk-eureka-server.log